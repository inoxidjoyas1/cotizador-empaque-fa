# Credenciales de SOLO LECTURA a Aspel SAE (usuario claude_ro @ SAE91).
# Copia este archivo como  .env  y llena los valores. NUNCA se sube a git.
# Solo se usa en la PC de la oficina, por refrescar_precios.py.

SAE_SERVER=127.0.0.1
SAE_PORT=1433
SAE_DB=SAE91
SAE_USER=claude_ro
SAE_PASSWORD=

# Lineas de empaque y lista de precios (no cambiar salvo que lo pidas).
LINEAS=F,A
LISTA_PRECIO=5
